# Release Notes

## 0.1.0-preview

- Renamed the project and public repo to `CodecMapper`
- Added the typed pipeline schema DSL
- Added JSON and XML codecs from one schema model
- Added .NET bridge importers for `System.Text.Json`, `Newtonsoft.Json`, and `DataContract`
- Added public getting-started, config-contract, and API reference docs
