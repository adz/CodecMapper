# Tasks

This file tracks the active forward-looking queue for `CodecMapper`.

Completed migration and parser work was folded into [docs/AGENT_NOTES.md](docs/AGENT_NOTES.md) and [AGENTS.md](AGENTS.md) so this file stays short.

- [x] **Task 11: Build a shared comparison harness against the old `CodecMapper`**
  - The previous repo is cloned locally at `benchmarks/CodecMapper/` for reference and benchmarking.
  - Do not rely on its published benchmark snapshot directly; its benchmark payload is `Person list` with 1000 records, while `CodecMapper` currently publishes a small single-object benchmark.
  - Build one manual harness that benchmarks the same payload through:
    - current `CodecMapper`
    - old `CodecMapper`
    - `System.Text.Json`
    - `Newtonsoft.Json`
  - Use that result for any serious performance comparison or naming decision.
  - *Done:* Added `src/CodecMapper.CompareRunner/` with a shared 1000-record nested-list payload.
  - *Latest local comparison:* `CodecMapper` encode `769925.5 ns/op`, old `CodecMapper` encode `781210.3 ns/op`; `CodecMapper` decode bytes `1971350.4 ns/op`, old `CodecMapper` decode stream `2132364.1 ns/op`.
  - *Notes:* Current `CodecMapper` is slightly faster on this shared payload, but the old `CodecMapper` allocates less on both encode and decode.

- [ ] **Task 12: Design a .NET-only C# attribute bridge**
  - Support importing a conservative subset of `System.Text.Json` and `Newtonsoft.Json` attributes into `CodecMapper` schemas.
  - Keep this out of the core package; reflection/codegen tradeoffs should be explicit.
  - Define the supported attribute matrix and the unsupported cases up front.

- [x] **Task 13: Rename `cmap` to `CodecMapper`**
  - Decide based on:
    - the shared benchmark comparison
    - whether the old repo contributes code worth merging versus only naming/history
    - package/repo/docs migration cost
    - whether the old preview repo should be superseded or redirected
  - Only rename once the comparison and migration story are concrete.
  - *Done:* The old experimental repo was renamed and archived, legacy assets were preserved under `docs/legacy-codemapper/`, and this repo was renamed locally to `CodecMapper`.
  - *Notes:* The shared comparison harness showed the old implementation was not technically superior enough to justify the rename on performance alone. The rename is for product clarity, not because the old code won.

- [ ] **Task 14: If needed, isolate the remaining BenchmarkDotNet child-project failure**
  - The benchmark app is runnable again, but the autogenerated child project still fails under `.NET SDK 10.0.103`.
  - This is no longer blocking day-to-day benchmarking because the manual runner works.
  - Revisit only if publication-quality BenchmarkDotNet output becomes important again.
