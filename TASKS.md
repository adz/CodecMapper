# Tasks

This file tracks the active forward-looking queue for `cmap`.

Completed migration and parser work was folded into [docs/AGENT_NOTES.md](/home/adam/projects/cmap/docs/AGENT_NOTES.md) and [AGENTS.md](/home/adam/projects/cmap/AGENTS.md) so this file stays short.

- [x] **Task 11: Build a shared comparison harness against the old `CodecMapper`**
  - The previous repo is cloned locally at `benchmarks/CodecMapper/` for reference and benchmarking.
  - Do not rely on its published benchmark snapshot directly; its benchmark payload is `Person list` with 1000 records, while `cmap` currently publishes a small single-object benchmark.
  - Build one manual harness that benchmarks the same payload through:
    - current `cmap`
    - old `CodecMapper`
    - `System.Text.Json`
    - `Newtonsoft.Json`
  - Use that result for any serious performance comparison or naming decision.
  - *Done:* Added `src/cmap.CompareRunner/` with a shared 1000-record nested-list payload.
  - *Latest local comparison:* `cmap` encode `769925.5 ns/op`, old `CodecMapper` encode `781210.3 ns/op`; `cmap` decode bytes `1971350.4 ns/op`, old `CodecMapper` decode stream `2132364.1 ns/op`.
  - *Notes:* Current `cmap` is slightly faster on this shared payload, but the old `CodecMapper` allocates less on both encode and decode.

- [ ] **Task 12: Design a .NET-only C# attribute bridge**
  - Support importing a conservative subset of `System.Text.Json` and `Newtonsoft.Json` attributes into `cmap` schemas.
  - Keep this out of the core package; reflection/codegen tradeoffs should be explicit.
  - Define the supported attribute matrix and the unsupported cases up front.

- [ ] **Task 13: Evaluate renaming `cmap` to `CodecMapper`**
  - Decide based on:
    - the shared benchmark comparison
    - whether the old repo contributes code worth merging versus only naming/history
    - package/repo/docs migration cost
    - whether the old preview repo should be superseded or redirected
  - Only rename once the comparison and migration story are concrete.
  - *Current read:* The old repo does not appear technically superior enough to justify a rename by itself. If the rename happens, it should be for product clarity and migration strategy, not because the old implementation wins.

- [ ] **Task 14: If needed, isolate the remaining BenchmarkDotNet child-project failure**
  - The benchmark app is runnable again, but the autogenerated child project still fails under `.NET SDK 10.0.103`.
  - This is no longer blocking day-to-day benchmarking because the manual runner works.
  - Revisit only if publication-quality BenchmarkDotNet output becomes important again.
